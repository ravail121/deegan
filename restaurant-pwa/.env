# API Configuration - Using Relative URLs with Vite Proxy
# BASE_URL is empty string - Vite proxy handles API routing to localhost:8000

# Environment
NODE_ENV=development

# Vite Dev Server Port (must match ngrok)
VITE_PORT=3000
